language: go

go:
- 1.12.8

os: linux
dist: focal

git:
  depth: false

install:
  - git clone --branch=ssh-test git@github.com:Prabhav-Thali/che-machine-exec.git
  - cd che-machine-exec
  - git checkout -qf $TRAVIS_COMMIT

before_install:
  - openssl aes-256-cbc -K $encrypted_bea0181439c1_key -iv $encrypted_bea0181439c1_iv -in git_deploy_key.enc -out /tmp/git_deploy_key -d 
  # Make sure only the current user can read the private key
  - chmod 600 /tmp/git_deploy_key
  # Create a script to return the passphrase environment variable to ssh-add
  - echo 'echo ${SSH_PASSPHRASE}' > /tmp/askpass && chmod +x /tmp/askpass
  # Start the authentication agent
  - eval "$(ssh-agent -s)"
  # Add the key to the authentication agent
  - DISPLAY=":0.0" SSH_ASKPASS="/tmp/askpass" setsid ssh-add /tmp/git_deploy_key </dev/null
  # - git remote set-url origin git@github.com:${TRAVIS_REPO_SLUG}.git
  # - echo $TRAVIS_REPO_SLUG

jobs:
  include:
   - stage: Create file
     if: type = push
     script:
     - |
       echo "Hello" > Hello.txt
       git add .
       git status
       git remote -v
       git commit -m "new file"
       git status
       pwd
       git push origin ssh-test
